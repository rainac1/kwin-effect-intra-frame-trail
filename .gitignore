# Build trees
/build/
/build-*/

# Local development sysroot and scratch checkouts
/.sysroot/
/.tools/

# Local install prefix and nested session state (see helpers/run-nested.sh)
/.local/
/.runtime/
